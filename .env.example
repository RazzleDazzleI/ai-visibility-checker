# Google Gemini — used on every check. Grounding is included on the free tier.
GEMINI_API_KEY=

# OpenAI — optional. Only used when USE_OPENAI=1, because it bills per call.
OPENAI_API_KEY=
USE_OPENAI=0

PORT=3001
DB_PATH=data/visibility.db
